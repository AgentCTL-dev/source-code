# Generated by `cargo run -p agentctl-crdgen` — do not edit by hand.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: mcpserversets.agentctl.dev
spec:
  group: agentctl.dev
  names:
    categories:
    - agentctl
    kind: MCPServerSet
    plural: mcpserversets
    shortNames:
    - mcpset
    singular: mcpserverset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.serverCount
      name: Servers
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for MCPServerSetSpec via `CustomResource`
        properties:
          spec:
            description: |-
              A reusable, namespaced bundle of MCP tool servers an `Agent`/`AgentFleet`
              binds via `spec.mcpServers`. Agents hold **NO tool-server
              credentials**: they never hold a tool server's token. The
              control plane brokers every remote MCP server through a credential-injecting,
              attesting, policy-enforcing proxy (the `MCPGateway`, the tool-plane analog of
              the `ModelGateway`) configured by this CRD. Each server names its remote
              endpoint, its auth (a `Secret`-backed token held at the gateway, never the
              pod), its per-tool trifecta tags, and an optional call budget.
            properties:
              servers:
                description: The MCP tool servers this set bundles.
                items:
                  description: One remote MCP tool server, brokered by the MCPGateway.
                  properties:
                    auth:
                      description: |-
                        How the gateway authenticates to the remote server. The credential lives
                        in a `Secret` the gateway reads; it is NEVER placed on the agent pod.
                      nullable: true
                      properties:
                        header:
                          description: |-
                            Header name to carry the token (default `Authorization` as
                            `Bearer <value>`). A custom header (e.g. `X-API-Key`) sends the raw value.
                          nullable: true
                          type: string
                        mode:
                          default: none
                          description: |-
                            Auth mode. `none` (unauthenticated server) | `staticToken` (a bearer the
                            gateway attaches upstream).
                          enum:
                          - none
                          - staticToken
                          type: string
                        tokenSecretRef:
                          description: |-
                            The bearer credential for `staticToken` mode: a `Secret` key the gateway
                            reads and attaches as `Authorization: Bearer <value>` on the upstream
                            hop. Required iff `mode == staticToken`. NEVER placed on the agent pod.
                          nullable: true
                          properties:
                            key:
                              description: The key within the `Secret`'s data holding the provider API key.
                              type: string
                            name:
                              description: The `Secret` name (same namespace as the `ModelPool`).
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                    budget:
                      description: Optional per-server call budget (enforced by the gateway).
                      nullable: true
                      properties:
                        maxTokens:
                          description: Maximum total tokens the pool may consume.
                          format: int64
                          type: integer
                      required:
                      - maxTokens
                      type: object
                    endpoint:
                      description: |-
                        The remote MCP server URL (Streamable HTTP transport). The AGENT never
                        dials this — it dials the gateway, which dials this.
                      type: string
                    name:
                      description: |-
                        Server name — the key an `Agent` references and the gateway facade path
                        segment (`/s/<name>`). Unique within the resolved `Agent` union.
                      type: string
                    tags:
                      description: |-
                        Per-tool trifecta capability tags the operator declares for the
                        Rule-of-Two check. A bare list is shorthand for the `*` glob.
                      items:
                        type: string
                      type: array
                  required:
                  - endpoint
                  - name
                  type: object
                type: array
            required:
            - servers
            type: object
            x-kubernetes-validations:
            - message: every server needs a non-empty name
              rule: self.servers.all(s, s.name != '')
            - message: each server endpoint must be an http(s):// URL
              rule: self.servers.all(s, s.endpoint.startsWith('https://') || s.endpoint.startsWith('http://'))
          status:
            description: '`MCPServerSet.status` — resolution + per-server broker health.'
            nullable: true
            properties:
              conditions:
                description: |-
                  Health conditions (`Ready`, …); shares the `conditions` + `Ready`-column
                  idiom with `Agent`/`AgentFleet`.
                items:
                  description: A status condition. Mirrors `metav1.Condition` shape.
                  properties:
                    lastTransitionTime:
                      description: An ISO 8601 / UTC timestamp.
                      nullable: true
                      type: string
                    message:
                      nullable: true
                      type: string
                    observedGeneration:
                      format: int64
                      nullable: true
                      type: integer
                    reason:
                      nullable: true
                      type: string
                    status:
                      description: '`"True"` | `"False"` | `"Unknown"`.'
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              serverCount:
                description: Number of servers in the set (printer column).
                format: int64
                nullable: true
                type: integer
              tagUnion:
                description: |-
                  The union of trifecta tags across all servers (informational; the
                  Agent-level union is the gate).
                items:
                  type: string
                type: array
            type: object
        required:
        - spec
        title: MCPServerSet
        type: object
    served: true
    storage: true
    subresources:
      status: {}
