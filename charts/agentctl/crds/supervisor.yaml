# Generated by `cargo run -p agentctl-crdgen` — do not edit by hand.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: supervisors.agentctl.dev
spec:
  group: agentctl.dev
  names:
    categories:
    - agentctl
    kind: Supervisor
    plural: supervisors
    shortNames: []
    singular: supervisor
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.user
      name: User
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for SupervisorSpec via `CustomResource`
        properties:
          spec:
            description: |-
              A user's supervisor agent (RFC 0027 §2): ensured on first authenticated
              request (org policy `supervisors: auto`), rendered into an `Agent` with
              the class's supervisor profile.
            properties:
              budgetOverride:
                description: Budget narrowing below the class ceiling.
                nullable: true
                properties:
                  lifetimeTokens:
                    format: int64
                    nullable: true
                    type: integer
                  windows:
                    items:
                      properties:
                        per:
                          description: '`hour` | `day` | `week` | `month`.'
                          type: string
                        tokens:
                          format: int64
                          type: integer
                      required:
                      - per
                      - tokens
                      type: object
                    type: array
                type: object
              instructionOverride:
                description: |-
                  PROSE-ONLY user instruction layer (never config-defining directives —
                  the renderer strips machinery; RFC 0027 §4).
                nullable: true
                type: string
              paused:
                default: false
                type: boolean
              user:
                description: The canonicalized IdP subject this supervisor serves.
                type: string
            required:
            - user
            type: object
          status:
            nullable: true
            properties:
              agentRef:
                description: The rendered Agent's name.
                nullable: true
                type: string
              conditions:
                items:
                  description: A status condition. Mirrors `metav1.Condition` shape.
                  properties:
                    lastTransitionTime:
                      description: An ISO 8601 / UTC timestamp.
                      nullable: true
                      type: string
                    message:
                      nullable: true
                      type: string
                    observedGeneration:
                      format: int64
                      nullable: true
                      type: integer
                    reason:
                      nullable: true
                      type: string
                    status:
                      description: '`"True"` | `"False"` | `"Unknown"`.'
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              lastConversation:
                nullable: true
                type: string
              ownerGroups:
                description: |-
                  The owner's identity-resolved groups, stamped by the GATEWAY at each
                  introspection (P4-2). The control MCP evaluates org accessPolicies
                  against these — the supervisor never asserts its owner's groups
                  itself, and stale grants age out at the owner's next conversation.
                items:
                  type: string
                type: array
              phase:
                nullable: true
                type: string
            type: object
        required:
        - spec
        title: Supervisor
        type: object
    served: true
    storage: true
    subresources:
      status: {}
