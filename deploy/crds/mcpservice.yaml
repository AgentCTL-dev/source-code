# Generated by `cargo run -p agentctl-crdgen` — do not edit by hand.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: mcpservices.agentctl.dev
spec:
  group: agentctl.dev
  names:
    categories:
    - agentctl
    kind: MCPService
    plural: mcpservices
    shortNames: []
    singular: mcpservice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.kind
      name: Kind
      type: string
    - jsonPath: .spec.endpoint
      name: Endpoint
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for MCPServiceSpec via `CustomResource`
        properties:
          spec:
            description: |-
              A capability registry entry (RFC 0032 §3): an MCP/peer/http service with
              tags (FLOORS), allow/exclude (ceilings), and the auth mode agents reach it
              with. Valid at every scope, user included.
            properties:
              allow:
                description: Tool-name ceiling (consumers narrow, never widen).
                items:
                  type: string
                type: array
              auth:
                description: |-
                  How agents authenticate: `service` (shared credential), `obo`
                  (per-user token via the exchange, RFC 0030), `passthrough`.
                nullable: true
                properties:
                  audience:
                    description: RFC 8707 resource/audience for minted tokens.
                    nullable: true
                    type: string
                  mode:
                    description: '`service` | `obo` | `passthrough`.'
                    type: string
                  tokenSecretRef:
                    description: 'Secret holding the service credential (`mode: service`).'
                    nullable: true
                    properties:
                      key:
                        description: The key within the `Secret`'s data holding the credential.
                        type: string
                      name:
                        description: The `Secret` name (same namespace as the referencing resource).
                        type: string
                    required:
                    - key
                    - name
                    type: object
                required:
                - mode
                type: object
              deploymentRef:
                description: |-
                  An in-cluster Deployment/Service this entry fronts (the operator
                  resolves the endpoint).
                nullable: true
                type: string
              direct:
                default: false
                description: |-
                  Dial directly from the agent pod (RFC 0024 posture) instead of
                  through the tenant mcpg.
                type: boolean
              endpoint:
                description: The service endpoint (`https://…`).
                nullable: true
                type: string
              exclude:
                items:
                  type: string
                type: array
              kind:
                default: mcp
                description: |-
                  `mcp` | `peer` | `http`. NOTE: agentd 1.3.1's services catalog accepts
                  ONLY `mcp` (schema: "phase A") — the renderer projects `peer`/`http`
                  entries by other means until upstream ships the other kinds (U5).
                enum:
                - mcp
                - peer
                - http
                type: string
              rate:
                description: Default arrival rate (`<burst>/<per>`).
                nullable: true
                type: string
              tags:
                description: |-
                  Capability tags — UNCONDITIONAL FLOORS (agentd treats catalog tags the
                  same way): `egress`, `secrets`, `exec`, `untrusted-content`, …
                items:
                  type: string
                type: array
            type: object
          status:
            nullable: true
            properties:
              conditions:
                items:
                  description: A status condition. Mirrors `metav1.Condition` shape.
                  properties:
                    lastTransitionTime:
                      description: An ISO 8601 / UTC timestamp.
                      nullable: true
                      type: string
                    message:
                      nullable: true
                      type: string
                    observedGeneration:
                      format: int64
                      nullable: true
                      type: integer
                    reason:
                      nullable: true
                      type: string
                    status:
                      description: '`"True"` | `"False"` | `"Unknown"`.'
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              consumers:
                description: |-
                  Agents currently granted this service (the tag-laundering guard
                  consults it: an edit that would widen a live consumer is refused).
                items:
                  type: string
                type: array
              phase:
                nullable: true
                type: string
            type: object
        required:
        - spec
        title: MCPService
        type: object
    served: true
    storage: true
    subresources:
      status: {}
