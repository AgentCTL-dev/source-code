# Generated by `cargo run -p agentctl-crdgen` — do not edit by hand.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: organizations.agentctl.dev
spec:
  group: agentctl.dev
  names:
    categories:
    - agentctl
    kind: Organization
    plural: organizations
    shortNames:
    - org
    - orgs
    singular: organization
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: Display
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.namespaces
      name: Namespaces
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for OrganizationSpec via `CustomResource`
        properties:
          spec:
            description: 'One tenant: namespaces, quotas, IdP binding, and access policy.'
            properties:
              accessPolicies:
                description: |-
                  IdP claims/groups → roles over label selectors. Evaluated top-down;
                  EVERY matching rule grants (a principal holds the union of its grants).
                items:
                  description: |-
                    One policy rule: who ([`PolicyMatch`]) gets which [`Role`] over which
                    agents ([`selector`](AccessPolicy::selector)).
                  properties:
                    match:
                      description: |-
                        Principal matcher. Rules with BOTH groups and claims require both to hold;
                        within `groups`, ANY listed pattern matching ANY held group suffices.
                      properties:
                        claims:
                          additionalProperties:
                            type: string
                          description: |-
                            Exact claim requirements (claim → required value; a list-valued claim
                            matches when it contains the value).
                          type: object
                        groups:
                          description: Group patterns; `*` is a wildcard (e.g. `okta:eng-*`).
                          items:
                            type: string
                          type: array
                      type: object
                    role:
                      description: |-
                        Roles, weakest → strongest. `viewer` reads specs/status; `operator` adds
                        converse + lifecycle verbs; `admin` adds CRUD, registry writes, org
                        settings. Token scopes may narrow a session BELOW the policy ceiling,
                        never above it.
                      enum:
                      - viewer
                      - operator
                      - admin
                      type: string
                    selector:
                      description: |-
                        Label selector over agents this grant covers. An empty/absent selector
                        covers everything in the org.
                      nullable: true
                      properties:
                        matchLabels:
                          additionalProperties:
                            type: string
                          type: object
                      type: object
                  required:
                  - match
                  - role
                  type: object
                type: array
              displayName:
                description: Human-facing name (listings, dashboards).
                nullable: true
                type: string
              gatewayHosts:
                description: Optional vanity hosts for the tenant gateway endpoints.
                nullable: true
                properties:
                  a2a:
                    nullable: true
                    type: string
                  hooks:
                    nullable: true
                    type: string
                type: object
              identity:
                description: |-
                  IdP binding: which issuers authenticate this org's members and how
                  their claims map to user/groups.
                nullable: true
                properties:
                  claimMappings:
                    description: 'Claim names carrying the user id and group list (defaults: sub/groups).'
                    nullable: true
                    properties:
                      groups:
                        default: groups
                        description: Claim carrying group memberships. Default `groups`.
                        type: string
                      user:
                        default: sub
                        description: Claim carrying the stable user id. Default `sub`.
                        type: string
                    type: object
                  providers:
                    description: |-
                      Issuers whose tokens authenticate members of this org. Matched against
                      the identity service's configured providers by issuer URL.
                    items:
                      properties:
                        clientRef:
                          description: |-
                            Optional reference to a client registration (Secret name) for
                            org-specific client credentials.
                          nullable: true
                          type: string
                        issuer:
                          description: Issuer URL (must be one the identity service federates).
                          type: string
                      required:
                      - issuer
                      type: object
                    type: array
                type: object
              namespaces:
                description: |-
                  Namespace management. Default: managed (`org-<name>` is created and
                  owned by the Organization — deleting the org deletes its namespaces).
                nullable: true
                properties:
                  extra:
                    description: Additional managed namespaces beyond the conventional `org-<name>`.
                    items:
                      type: string
                    type: array
                  mode:
                    default: managed
                    description: |-
                      managed (default): the operator creates/owns `org-<name>` (+ `extra`).
                      unmanaged: namespaces are the platform team's problem; the org only
                      scopes identity/policy.
                    enum:
                    - managed
                    - unmanaged
                    type: string
                type: object
              quotas:
                description: |-
                  Tenant ceilings. `agents` is enforced as a ResourceQuota on the managed
                  namespaces; the metering ceilings (tokens/day, sandbox CPU) are recorded
                  for the billing plane (RFC 0026 §12) and enforced there.
                nullable: true
                properties:
                  agents:
                    description: |-
                      Ceiling on Agent objects per managed namespace (ResourceQuota
                      `count/agents.agentctl.dev`).
                    format: int64
                    nullable: true
                    type: integer
                  sandboxCpuSeconds:
                    description: 'Metering ceiling: sandbox CPU seconds (billing plane, not a K8s quota).'
                    format: int64
                    nullable: true
                    type: integer
                  tokensPerDay:
                    description: 'Metering ceiling: model tokens per day (billing plane, not a K8s quota).'
                    format: int64
                    nullable: true
                    type: integer
                type: object
              registryScope:
                description: Registry scoping (the org's defaults class; resolved in P2).
                nullable: true
                properties:
                  classRef:
                    nullable: true
                    type: string
                type: object
              supervisors:
                description: 'Supervisor provisioning for members: auto (default) | manual | disabled.'
                enum:
                - auto
                - manual
                - disabled
                - null
                nullable: true
                type: string
            type: object
          status:
            nullable: true
            properties:
              conditions:
                items:
                  description: A status condition. Mirrors `metav1.Condition` shape.
                  properties:
                    lastTransitionTime:
                      description: An ISO 8601 / UTC timestamp.
                      nullable: true
                      type: string
                    message:
                      nullable: true
                      type: string
                    observedGeneration:
                      format: int64
                      nullable: true
                      type: integer
                    reason:
                      nullable: true
                      type: string
                    status:
                      description: '`"True"` | `"False"` | `"Unknown"`.'
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              namespaces:
                description: Managed namespaces the operator has reconciled.
                items:
                  type: string
                type: array
              observedGeneration:
                format: int64
                nullable: true
                type: integer
              phase:
                description: 'Coarse phase: `Provisioning` | `Ready` | `Degraded`.'
                nullable: true
                type: string
            type: object
        required:
        - spec
        title: Organization
        type: object
    served: true
    storage: true
    subresources:
      status: {}
