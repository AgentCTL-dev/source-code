# Generated by `cargo run -p agentctl-crdgen` — do not edit by hand.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: agentclasses.agentctl.dev
spec:
  group: agentctl.dev
  names:
    categories:
    - agentctl
    kind: AgentClass
    plural: agentclasses
    shortNames: []
    singular: agentclass
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for AgentClassSpec via `CustomResource`
        properties:
          spec:
            description: |-
              The scoped defaults bundle (RFC 0032 §3): settings fragments, service
              grants, bundle refs, subagent templates, the supervisor profile, HITL
              channels — and the SECURITY FLOORS lower scopes may only narrow.
            properties:
              defaults:
                description: Content defaults (shadow-by-name downward).
                nullable: true
                properties:
                  approval:
                    nullable: true
                    properties:
                      hitl:
                        description: HITL channels (`slack:#support-approvals`).
                        items:
                          type: string
                        type: array
                      policy:
                        description: '`ask` (gate to the acting human) | `auto` | `deny`.'
                        nullable: true
                        type: string
                    type: object
                  budget:
                    nullable: true
                    properties:
                      lifetimeTokens:
                        format: int64
                        nullable: true
                        type: integer
                      windows:
                        items:
                          properties:
                            per:
                              description: '`hour` | `day` | `week` | `month`.'
                              type: string
                            tokens:
                              format: int64
                              type: integer
                          required:
                          - per
                          - tokens
                          type: object
                        type: array
                    type: object
                  intelligence:
                    nullable: true
                    properties:
                      budget:
                        description: Token budget (lifetime + windows; narrows the class ceiling).
                        nullable: true
                        properties:
                          lifetimeTokens:
                            format: int64
                            nullable: true
                            type: integer
                          windows:
                            items:
                              properties:
                                per:
                                  description: '`hour` | `day` | `week` | `month`.'
                                  type: string
                                tokens:
                                  format: int64
                                  type: integer
                              required:
                              - per
                              - tokens
                              type: object
                            type: array
                        type: object
                      model:
                        description: Explicit model id (wins over tier).
                        nullable: true
                        type: string
                      pool:
                        description: The `ModelPool` binding.
                        nullable: true
                        type: string
                      tier:
                        description: Model tier within the pool (`small` | `large` | pool-defined).
                        nullable: true
                        type: string
                    type: object
                  limits:
                    description: Resolved limits/budgets (subset; additive).
                    nullable: true
                    properties:
                      lifetimeTokens:
                        description: |-
                          Per-INSTANCE lifetime token budget — cumulative across every run/reaction
                          of this instance (RFC 0025). Renders `--budget-tokens-lifetime`. On a
                          bounded `once` run exhaustion folds into `EXIT_BUDGET(7)`; a
                          `reactive`/`loop`/`schedule` daemon stops accepting new reactions and
                          drains cleanly (exit 0 by default, operator-tunable via the agent's
                          `--budget-exit-code`). Each instance (fleet member included) gets its own
                          lifetime box — the harness is the only token-budget enforcement point now
                          that the metering gateway is gone.
                        format: uint64
                        minimum: 0.0
                        nullable: true
                        type: integer
                      maxDepth:
                        format: uint64
                        minimum: 0.0
                        nullable: true
                        type: integer
                      maxSteps:
                        format: uint64
                        minimum: 0.0
                        nullable: true
                        type: integer
                      maxTokens:
                        description: Per-RUN token box (each once-run / each reaction). Renders `--max-tokens`.
                        format: uint64
                        minimum: 0.0
                        nullable: true
                        type: integer
                    type: object
                  priority:
                    enum:
                    - low
                    - normal
                    - high
                    - null
                    nullable: true
                    type: string
                  runtime:
                    nullable: true
                    properties:
                      image:
                        description: Explicit image reference (wins over version resolution).
                        nullable: true
                        type: string
                      version:
                        description: Runtime version tag (resolved to a digest via class policy).
                        nullable: true
                        type: string
                    type: object
                  store:
                    nullable: true
                    properties:
                      class:
                        default: ephemeral
                        description: '`managed` (state service) | `local` (PVC) | `ephemeral` (emptyDir).'
                        enum:
                        - ephemeral
                        - local
                        - managed
                        type: string
                      retention:
                        description: Retention window for `delete` (`720h`).
                        nullable: true
                        type: string
                      size:
                        description: '`local` class only: the PVC size (default `1Gi`).'
                        nullable: true
                        type: string
                    type: object
                type: object
              floors:
                description: |-
                  SECURITY FLOORS — lower scopes narrow only (RFC 0032 §2); widening is
                  an admission error naming the floor.
                nullable: true
                properties:
                  approval:
                    description: Approval may not be weakened below this policy.
                    nullable: true
                    type: string
                  budget:
                    description: The budget ceiling (agents may only shrink it).
                    nullable: true
                    properties:
                      lifetimeTokens:
                        format: int64
                        nullable: true
                        type: integer
                      windows:
                        items:
                          properties:
                            per:
                              description: '`hour` | `day` | `week` | `month`.'
                              type: string
                            tokens:
                              format: int64
                              type: integer
                          required:
                          - per
                          - tokens
                          type: object
                        type: array
                    type: object
                  capabilities:
                    description: Trifecta legs an agent may at most hold (an unlisted leg is refused).
                    nullable: true
                    properties:
                      egress:
                        description: |-
                          The agent requests outbound network egress. Declared intent; gated at
                          admission (a trifecta leg), never wired by the operator.
                        nullable: true
                        type: boolean
                      exec:
                        description: |-
                          The agent requests in-sandbox command execution. Declared intent; gated at
                          admission (a privileged trifecta leg), never wired by the operator.
                        nullable: true
                        type: boolean
                      secrets:
                        description: |-
                          The namespace-local `Secret` names the agent may read. Declared intent;
                          each name is validated at admission (a trifecta leg); the operator does
                          not itself mount them.
                        items:
                          type: string
                        nullable: true
                        type: array
                    type: object
                  egress:
                    description: '`closed` forbids any egress not named by a granted service.'
                    nullable: true
                    type: string
                  tools:
                    description: Tool-name patterns agents may at most be allowed.
                    items:
                      type: string
                    type: array
                type: object
              groupSelector:
                description: |-
                  Group scoping: this class applies to principals matching the selector
                  (an `AgentClass` with a groupSelector IS the group scope).
                items:
                  type: string
                type: array
              hitl:
                description: HITL notifier channels.
                items:
                  type: string
                type: array
              parent:
                description: |-
                  The parent class in the scope chain (system → org → group). Absent ⇒
                  this is a chain root.
                nullable: true
                type: string
              services:
                description: Service grants agents of this class may draw on.
                items:
                  description: |-
                    A grant against a registry `MCPService`: the agent may narrow the entry's
                    ceilings (allow ⊆ registry allow), never widen — the P2-2 resolver and
                    admission both enforce it.
                  properties:
                    allow:
                      description: Tool-name patterns this agent may call (narrows the entry's allow).
                      items:
                        type: string
                      type: array
                    name:
                      description: The `MCPService` name (scope-chain resolved).
                      type: string
                  required:
                  - name
                  type: object
                type: array
              skillSets:
                items:
                  type: string
                type: array
              supervisor:
                description: The supervisor profile agents-of-users in this class get (RFC 0027).
                nullable: true
                properties:
                  budget:
                    nullable: true
                    properties:
                      lifetimeTokens:
                        format: int64
                        nullable: true
                        type: integer
                      windows:
                        items:
                          properties:
                            per:
                              description: '`hour` | `day` | `week` | `month`.'
                              type: string
                            tokens:
                              format: int64
                              type: integer
                          required:
                          - per
                          - tokens
                          type: object
                        type: array
                    type: object
                  instruction:
                    description: The platform instruction layer (users override with PROSE ONLY).
                    nullable: true
                    type: string
                  services:
                    description: Services the supervisor itself is granted (the control MCP etc.).
                    items:
                      description: |-
                        A grant against a registry `MCPService`: the agent may narrow the entry's
                        ceilings (allow ⊆ registry allow), never widen — the P2-2 resolver and
                        admission both enforce it.
                      properties:
                        allow:
                          description: Tool-name patterns this agent may call (narrows the entry's allow).
                          items:
                            type: string
                          type: array
                        name:
                          description: The `MCPService` name (scope-chain resolved).
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                type: object
              workflowSets:
                description: Workflow/skill bundle refs.
                items:
                  type: string
                type: array
            type: object
        required:
        - spec
        title: AgentClass
        type: object
    served: true
    storage: true
    subresources: {}
