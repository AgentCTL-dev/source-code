# Generated by `cargo run -p agentctl-crdgen` — do not edit by hand.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: modelpools.agentctl.dev
spec:
  group: agentctl.dev
  names:
    categories:
    - agentctl
    kind: ModelPool
    plural: modelpools
    shortNames:
    - mp
    singular: modelpool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.provider
      name: Provider
      type: string
    - jsonPath: .spec.endpoint
      name: Endpoint
      priority: 1
      type: string
    - jsonPath: .spec.budget.maxTokens
      name: Budget
      type: integer
    - jsonPath: .status.usedTokens
      name: Used
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for ModelPoolSpec via `CustomResource`
        properties:
          spec:
            description: |-
              A pool of model access (the intelligence plane). Agents hold **NO provider
              secrets**; they dial the control plane keyless, and the control plane supplies
              model access through a credential-injecting, metering, budget-enforcing proxy
              (the `ModelGateway`) configured by this CRD. The pool names the
              provider, its endpoint, the `Secret` holding the provider API key, the
              allowed models, and an optional total token budget.
            properties:
              budget:
                description: Total token budget for the pool (enforced by the gateway).
                nullable: true
                properties:
                  maxTokens:
                    description: Maximum total tokens the pool may consume.
                    format: int64
                    type: integer
                required:
                - maxTokens
                type: object
              credentialSecretRef:
                description: |-
                  Reference to the `Secret` holding the provider API key. The gateway
                  injects it; the agent never sees it.
                properties:
                  key:
                    description: The key within the `Secret`'s data holding the provider API key.
                    type: string
                  name:
                    description: The `Secret` name (same namespace as the `ModelPool`).
                    type: string
                required:
                - key
                - name
                type: object
              defaultModel:
                description: Default model id, used when a request does not pin one.
                nullable: true
                type: string
              endpoint:
                description: Provider base URL, e.g. `http://mock-provider.default:8080`.
                type: string
              models:
                description: Allowed model ids for this pool.
                items:
                  type: string
                type: array
              provider:
                description: Provider id, e.g. `"mock"` | `"anthropic"` | `"openai"` (free string).
                type: string
            required:
            - credentialSecretRef
            - endpoint
            - provider
            type: object
            x-kubernetes-validations:
            - message: budget.maxTokens must be > 0
              rule: '!has(self.budget) || self.budget.maxTokens > 0'
            - message: credentialSecretRef.name is required
              rule: self.credentialSecretRef.name != ''
          status:
            description: '`ModelPool.status` — running meter against the pool''s budget.'
            nullable: true
            properties:
              conditions:
                description: |-
                  Health conditions (`Ready`, …); shares the `conditions` + `Ready`-column
                  idiom with `Agent`/`AgentFleet`.
                items:
                  description: A status condition. Mirrors `metav1.Condition` shape.
                  properties:
                    lastTransitionTime:
                      description: An ISO 8601 / UTC timestamp.
                      nullable: true
                      type: string
                    message:
                      nullable: true
                      type: string
                    observedGeneration:
                      format: int64
                      nullable: true
                      type: integer
                    reason:
                      nullable: true
                      type: string
                    status:
                      description: '`"True"` | `"False"` | `"Unknown"`.'
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              usedTokens:
                description: Total tokens consumed against the pool so far.
                format: int64
                nullable: true
                type: integer
            type: object
        required:
        - spec
        title: ModelPool
        type: object
    served: true
    storage: true
    subresources:
      status: {}
