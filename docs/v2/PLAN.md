# agentctl v2 — execution plan and progress register

> **Living document.** Update the `Status` column as work lands; never delete
> rows — flip them to `Done`/`Dropped` with a date and a pointer (PR/commit).
> Statuses: `Todo` · `In progress` · `Blocked(<on>)` · `Done(<date>)` ·
> `Deferred` · `Dropped`. Owner column left blank until assigned.
> Companion: [`ARCHITECTURE.md`](ARCHITECTURE.md) (the design) · RFCs 0026–0035.

## Phase overview

| Phase | Theme | Gate (exit criterion) |
|---|---|---|
| **P0** | Re-integration: speak agentd v1.3.x at all | An `Agent` CR runs the real agentd v1.3.1 image end-to-end in e2e (provision → converse → drain), on the rendered-directory model |
| **P1** | Identity core + tenancy + gateway routing | `agentctl login` (device flow) → chat with a namespaced agent through the gateway as a real per-user principal |
| **P2** | Provisioning v2: CRDs, registry, projection, admission | v1alpha2 set served + converted; registry scopes render + validate via the binary; floors enforced |
| **P3** | Durability plane | `store.class: managed` on the state service passes the SIGKILL/restore matrix; backup/restore/migrate verbs work |
| **P4** | Supervisors + control MCP | First-login supervisor; "create an agent" via chat lands a working Agent, OBO-authorized |
| **P5** | Capability plane: tenant mcpg, OBO egress, sandbox, HITL | Governed tool call with per-user token injection; sandbox run; gate answered from Slack |
| **P6** | Fleets & scaling v2 | All three partition strategies green in e2e incl. guarded resize + scale-from-zero |
| **P7** | Productization: exposure, observability, managed-service profile | Webhook exposure GA; dashboards; usage export; docs site |
| **PM** | Multi-cluster (RFC 0035 leaves Draft) | Design review after P6; implementation phased separately |

Dependency spine: P0 → P1 → P2 → {P3, P4} → P5 → P6 → P7 → PM.
(P3 and P4 can proceed in parallel once P2's renderer exists; P4 needs P1's
identity + a minimal P3 state path for the supervisor's own store.)

## P0 — Re-integration (contract truth first)

| ID | Item | Definition of done | Status |
|---|---|---|---|
| P0-1 | **ACC v2 contract re-baseline** | `contract/` regenerated per RFC 0026 §3 (schemas, A2A profile, exit-intent table, metrics 1.2, env, store profile); fixtures captured from agentd v1.3.1 | **Done(2026-08-30)** — commit `0254d13`; +restart-only partition, +store checkpointer profile, +known-gap annotations (U3/U4) baked into manifest schema |
| P0-2 | **agent-contract-client rebuild** | negotiates via config-schema `x-agentd-contract-version` + probes; no `surfaces{}` dependency; version-skew tests | **Done(2026-08-30)** — commit `0254d13`; four-clock negotiation, vendored exit/metrics/restart-only tables compiled in, permissive Manifest reader, pre-rewrite agents identified as unmanageable |
| P0-3 | **Operator: rendered-directory launch path** | flags-era rendering deleted; `-c services.yaml -c agentd.yml` + folders; explicit `run_until`; probes/drain/podFailurePolicy per intent table | **Done(2026-08-30)** — commit `37af063`; new `agent-config` shared builder (proven via real `--validate-config`), single-document layer for now (the services.yaml catalog layer + conventional folders land with the registry, P2-3); shard fleets keep the guarded resize via the `agentctl.dev/shards` annotation |
| P0-4 | **Admission: binary validation step** | `agentd --validate-config` sandbox job wired; effective surfaces recorded in status | **Done(2026-08-30)** (rung) — in-webhook spawn_blocking run over the SAME composed document (shared builder), secret-ref placeholders auto-derived, binary's config.invalid diagnosis travels into the deny message; chart `admission.binaryValidation` (staging image needed until U9). Effective-surfaces recording moves to P2-3 (needs the services catalog) |
| P0-5 | **e2e vs real agentd** | mock-agent retired where possible; `mock:` intelligence used; provision→converse→drain→delete green in kind | **Done(2026-08-30)** — 20 scenarios GREEN against real agentd 1.3.1 in kind (provisioning 2, management 5 incl. drain/pause/cancel over the A2A root, shard 1 w/ store-fence identity, a2a 3 through the gateway, conformance 2, claim 4 incl. KEDA 0→N); 4 honest skips (sec-oidc/trusted-proxy: pre-existing unarmed-gate gap superseded by P1 authn scenarios; sec-netpol: Calico lane; sec-aauth: pending apd build). Live-run fixes folded back: explicit store for every shape (defaulted store hit the read-only rootfs, exit 6), TLS mock provider w/ HTTP/1.1 framing, capability probes load the pod's config, apiserver+gateway forward to the A2A root (the /mcp path is dead), gateway accepts the native PascalCase dialect, metrics registry reconciled against the LIVE exposition (+11 series, summary-suffix matching), KEDA chart-pin, ServiceMonitor capability guard, coordination attestation off in the direct-driving base lane. NOTE: the images-quick lane runs a host-built gnu binary on distroless/cc — the canonical musl/scratch image lane (images.sh) is CI's |
| P0-6 | **Upstream asks filed** (U1–U7 below) | issues filed with repro + proposal; tracked links here | **Done(2026-08-30)** — delivered to the upstream owners' working sessions with repro + proposals: U1–U5 as a formal batch to agentd (2026-08-30); U6 conveyed to mcpg (store façade alignment + sandbox 0022 status obtained); U7 carried by the standing RFC 0023–0025 asks; U8/U9 already resolved. Statuses tracked per-row below |

## P1 — Identity core, tenancy, gateway routing

| ID | Item | Definition of done | Status |
|---|---|---|---|
| P1-1 | `agentctl-identity` skeleton (axum, PG schema, KMS envelope) | deploys via chart; threat-model doc merged (RFC 0028 §8) | **Core done(2026-08-30)** — crate `agentctl-identity` (BUSL): env config w/ https-issuer enforcement, AES-256-GCM sealer (AAD context binding), Store trait w/ MemoryStore + PgStore (device_sessions + principals migrations, bearer-hash-only at rest), HTTP surface `/healthz` `/v1/providers` `/v1/device/*` `/v1/introspect` `/v1/principals/*` (constant-time admin gate, fail-closed with no token); chart `identity.service.*` (default-off, providers→IDENTITY_PROVIDERS JSON, store=postgres reuses the DATABASE_URL helper + pg CA pin, apiToken→IDENTITY_ADMIN_TOKEN), deploy/identity Dockerfile + release matrix + images-quick lane. Remaining for full DoD: threat-model doc (with P1-4/P1-5 wiring). **Deployed live 2026-08-30**: chart-rendered identity w/ store=postgres (migrations ran on bundled PG), /healthz ok, /v1/providers lists, /v1/introspect 403 fail-closed without the admin token |
| P1-2 | OIDC federation (Auth0/Keycloak/Okta/generic) + device flow | `agentctl login/whoami` against Keycloak (dev) and one SaaS IdP | **Core done(2026-08-30)** — Federation: issuer-pinned discovery (host-poisoning refused), JWKS cache w/ refresh-on-unknown-kid, aud/exp enforcement, provider-prefixed subjects + groups claim, RFC 8628 device start/poll; proven by an in-process mock-IdP integration suite (device e2e, wrong-aud refused, poisoned discovery refused). CLI `agentctl login` (device flow vs the service wire, opaque handle — device_code never leaves the service; session at `~/.config/agentctl/credentials.json` 0600, no refresh token client-side) + `agentctl whoami`. Remaining for full DoD: live Keycloak + one SaaS IdP run (needs a deployed stack — with P1-5 e2e) |
| P1-3 | `Organization` CRD + claim mapping + namespace/quota reconcile | org create → namespace, roles resolve from IdP groups | **Done(2026-08-30)** — cluster-scoped Organization at agentctl.dev/v1alpha2 (new type starts on the target version); pure access engine `agent_api::org::access` (group globs, claim equality/containment, union grants, role ordering, all-labels selector scope, empty-matcher-grants-nothing; the RFC worked example is a test); operator tenancy controller (managed namespaces labeled+owner-referenced → org delete GC-cascades; per-ns ResourceQuota `count/agents.agentctl.dev`, removed when the ceiling is dropped; CRD-presence gate keeps upgraded clusters healthy); **live e2e `org-tenancy` PASS** (ns+label+ownerRef, quota refuses agent #6 with 403 exceeded-quota, status Ready, ns GC on delete). Enforcement wiring is P1-8 |
| P1-4 | Per-(user,agent) principal minting + operator projection | principals hot-projected; addressed gate answerable only by named user in e2e | **Done(2026-08-30)** — `spec.access.principals[]` (subjects) → operator mints via identity `/v1/principals/mint` (admin-gated; Validated=False if identity unarmed), projects into the owner-referenced `<name>-principals` Secret (existing keys never re-minted; removed subjects pruned; Secret landed BEFORE the config references it — a dangling bearer_ref is startup exit 2 that `--validate-config` cannot catch), mounts it 0444, and binds `a2a.principals[]`: per-subject `{{secret-file:…}}` user rules FIRST then the control-plane `operator` san-rule LAST (first-match-wins + the gateway presents the CP client cert on every forward; any non-empty list disables agentd's implicit operator fallback). **Live e2e `a2a-principals-gate` PASS**: minted bearer converses as `user`, unmatched-cert anonymous + wrong bearer refused, CP cert still operator. CAVEAT vs the DoD word "hot": principals are NOT reload-free in agentd 1.3.1 (Resolver::build resolves bearer_refs once at startup; verified with the agentd session — our restart-only contract file corrected) → new user = rolling restart until upstream rebuilds the resolver on reload (+U1). Gateway per-session bearer injection lands in P1-5 |
| P1-5 | Gateway v2 routing core (`orgs/<org>/agents/<name>`, cards, SSE) | chat + streaming through gateway with injected principal; loopback-operator closed on all agents | **Done(2026-08-30)** — org route family (`POST /orgs/<org>/agents|fleets/<name>` + nested `.well-known/agent-card.json`): Organization CR → managed-namespace resolution (404 on unknown org, never guessed); inbound authn REQUIRED via identity `/v1/introspect` over a dedicated admin env pair (`AGENTCTL_IDENTITY_TOKEN` — deliberately not `AGENTCTL_API_TOKEN`, which arms the coarse gate; unarmed identity ⇒ 503, never open); the caller's per-(user,agent) bearer is fetched from the projected `<name>-principals` Secret and injected upstream in `forward_request` (declared-but-unlisted subject ⇒ 403; declared-but-not-yet-projected ⇒ 503 retryable, never silent operator promotion); caller identity forwarded as X-Auth-*. Loopback-operator CLOSED ON ALL AGENTS: every A2A-serving config now carries a principals list (at minimum the control-plane operator san-rule), disabling agentd's implicit loopback/management fallback fleet-wide. identity service gained `IDENTITY_ISSUER_CA` (private-CA IdPs — chart `identity.service.issuerCa` Secret/ConfigMap mount). **Live e2e `org-route-user` PASS**: signed mock-IdP token → introspection (JWKS fetched in-cluster over the chart CA) → principal injection → answered as `user:mock:alice`; streaming SSE through the org route; 401 no-token, 403 unlisted subject, 404 unknown org; tenant-scoped card. Deferred per RFC/PLAN split: handle resolution (P2-7), accessPolicies enforcement + origins/netpol legs (P1-8), SSE resume-cursor + watched routing cache + gateway task-store retirement (RFC 0029 §7, later wave) |
| P1-6 | AAuth provider role (federated enrollment via projected SA token, JWKS) | agentd enrolls secret-free at boot; token refresh observed across rotation | **Done(2026-08-30)** — agentctl-identity IS the fleet's Agent Provider, speaking the SHIPPED 1.3.1 client wire (verified in agentd source): `POST /enroll` + `POST /agent-token` (RFC 9421 `hwk` signatures — inline Ed25519 JWK, covered `@method @authority @path content-digest signature-key`, Content-Digest checked, ±300s freshness), public `/.well-known/aauth-agent.json` + `/aauth-jwks.json` for resource servers, and the operator's four `/admin/*` endpoints (allowed-keys register/delete, agents list, revoke) behind the shared admin bearer. Enrollment: allowlist-by-thumbprint (secret-free for the agent — it only signs with its own key), idempotent by jkt (deterministic `aauth:<local>@<domain>` ids); agent tokens are EdDSA `aa-agent+jwt` with PoP `cnf.jwk` binding; provider key from `IDENTITY_AAUTH_SEED` (chart `identity.service.aauth`). **sec-aauth UNSKIPPED and PASS live (43.8s)**: real agentd 1.3.1 provisioned→enrolled→token→3 signed MCP calls verified by the resource server against identity's JWKS; operator learned status.identity.aauth via /admin/agents. Doc corrections vs RFC folded: 1.3.1 enrolls LAZILY on first signed dial (not boot; no exit 4 — provider outage degrades to unsigned+401); the federated `enrollment_assertion` leg is accepted on the wire but deferred until the renderer projects SA tokens (upstream delta; the legacy apd fixture + image gate are deleted) |
| P1-7 | CLI verbs: `login/chat/get/describe/pause/resume/drain` | against P1 stack | **Done(2026-08-30)** — `login`/`whoami` (P1-2), `get`/`describe` (v1), plus: `chat <org>/<agent>` (gateway org route as the signed-in user — session bearer, reply-text extraction with JSON fallback, minimal stdin REPL; 401→"login again", 403→named-principal message) and `drain|lame-duck|pause|resume|cancel [--fleet]` (POSTs to the aggregated management.agentctl.dev API — rides kubeconfig + K8s RBAC like kubectl; never dials pods). Smoked LIVE against the P1 stack: `whoami` renders the session, `drain` returned the agent's own `{"state":"draining"}`, `chat` round-tripped through introspection + principal injection to a real agentd answer. Remaining niceties deferred: streaming chat output, `agentctl get orgs` |
| P1-8 | **Access policies** (IdP claims/groups/scopes → roles over label selectors) resolved by identity, enforced at gateway + apiserver + RBAC mirror | "eng operates `team: engineering`, marketing refused, admin sees all" — red/green e2e per enforcement point | **Done(2026-08-30)** — GATEWAY: org routes resolve the caller's grants from Organization.accessPolicies (pure engine `agent_api::org::access`, groups matching; claims leg engine-ready) and enforce role-over-labels per method BEFORE any principal work (SendMessage/cancel = operator; task reads/subscribes = viewer; unknown methods fail toward the stronger role; an org with NO policies imposes no scoping — policies restrict once stated). RBAC MIRROR: the operator projects the role ladder per managed namespace (viewer = read; operator = read + the aggregated management verbs as `agents/<verb>` create; admin = `*` on both groups) with RoleBindings from EXACT group names (globs cannot be K8s Group subjects; label-selector scope widens to the namespace in the mirror — precise scoping is the gateway's job; both limits documented); operator ClusterRole gains roles/rolebindings + escalate/bind. APISERVER leg = the mirror (management verbs already ride K8s RBAC through the aggregated API). **Live e2e `org-access-policy` PASS (18.9s)**: eng operates eng-bot 200, refused 403 on mkt-bot (converse AND view), marketing views its team 200 but cannot converse 403, admin operates both 200; the mirror's admin binding carries the exact group. P1 IS COMPLETE |

## P2 — Provisioning v2

| ID | Item | Definition of done | Status |
|---|---|---|---|
| P2-1 | v1alpha2 CRDs (Agent, AgentFleet, AgentTemplate, AgentClass, MCPService, Organization, Supervisor) + conversion webhook | schema tests; v1alpha1 objects convert with warnings | **Done(2026-08-30)** — `agent_api::v1alpha2` family: Agent (class/handle/runtime/shape + `triggers[]` typed union over ALL TEN agentd start kinds w/ exactly-one CEL, workflows/skills bundles, `services[]` grants, intelligence/store/identity/expose/peers/lifecycle/approval; v1-compat fields kept for lossless conversion), AgentFleet (v2 template), AgentTemplate (+params), AgentClass (defaults + narrow-only `floors` + supervisor profile), MCPService (tags-as-floors, allow/exclude, auth service|obo|passthrough, direct), Supervisor (RFC 0027 §2). Conversion: v1→v2 LOSSLESS (mode/subscribe/loop/schedule/workflow → shape+triggers+workflows; tested per mode + round-trip), v2→v1 lossy naming every dropped field; **v1alpha2 is the storage version**; deprecation warnings ride ADMISSION (ConversionReview has no warnings channel). crdgen merges multi-version CRDs (kube merge_crds) + injects the conversion-webhook stanza (admission `/convert`, cert-manager inject-ca-from; Helm crds/ untemplated ⇒ default-ns assumption documented). **Live**: all 8 CRDs applied; v1 write→v2 storage→read-back at BOTH versions verified; deprecation warning surfaced on a v1 write; **full catalogue 25 PASS / 0 fail with every operation through the conversion webhook**. Traps fixed en route: YAML-1.1 `on:`-is-boolean (event trigger field renamed `name`; RFC 0033 updated), CEL-reserved `loop` (`__loop__`), CEL rule-cost budget (sum-of-conditionals + triggers maxItems). Operator/gateway still speak v1alpha1 through conversion — their v2-native flip rides P2-3 |
| P2-2 | Registry resolution engine (scope chain, narrowing floors) | unit-tested resolver; widening attempts rejected naming the floor | **Done(2026-08-30)** — pure `agent_api::registry::resolve(chain, agent, services)`: content folds most-specific-wins (defaults shadow by field, bundles dedupe, agent spec wins); floors fold INTERSECTION-only down the chain (capability legs incl. the secrets-name-list leg, egress closure, budget ceilings incl. unbounded-exceeds-bounded, tool-pattern ceilings with conservative glob implication, approval rank) — a lower scope widening OR an agent requesting past the effective floor is a `Violation` NAMING the floor's holder, collected exhaustively (one denial names every defect). Service grants resolve against the registry slice: grant.allow ⊆ entry ceiling (violation names the MCPService), entry tags travel as inherited floors; closed egress refuses raw capabilities.egress without an egress-tagged grant AND inline mcpServers (registry bypass). Consumed by admission in P2-4, the projection in P2-3 |
| P2-3 | Projection compiler (services.yaml/agentd.yml/folders, vars, secrets refs) | renderer output validates via binary across the example matrix; hashes in status | **Done(2026-08-30)** (core) — `agent_config::Projection`: the services CATALOG layer (kind mcp, endpoint/tags-as-floors/credential headers — always emitted, empty catalog keeps argv uniform) + the instance layer whose `mcp.servers[]` REFERENCE entries (`service:` — reference-never-restate; the real binary confirmed the consumer view via `config.effective_server`: allow narrowed, tags inherited — captured as `contract/fixtures/config/services-{catalog,instance}.json` + `effective-server.ndjson`). Invocation `-c services.json -c agentd.json` everywhere: pod argv, admission's `run_validate_config` (byte-identical sandbox), the gated agent-config real-binary test. ConfigMap carries both keys; `Projection::hash()` spans both layers (pod annotation) and lands in **v1alpha2 `status.renderedHash`** (verified live). **Full catalogue 25 PASS / 0 fail with every agent booting the two-layer form.** Remaining leg (rides the bundle CRs): folder emission for WorkflowSet/SkillSet + `vars:` cross-layer variability (renderer rules recorded; consumed when P2's registry objects carry content) |
| P2-4 | Policy ladder (trifecta/egress/budget floors; tag-laundering guard) | admission e2e red/green cases | **Done(2026-08-30)** — admission FLIPPED TO THE v1alpha2 VIEW (webhook rules v1alpha2 + Equivalent up-converts v1 writes — the v1 view drops class/services, a real policy hole): normalize_view splits every request into the v2 spec (ladder) + the reconstructed v1 down-view (legacy rungs, byte-compatible); the ladder fetches the AgentClass parent chain (depth-capped, named-class-missing fails closed, transient reads fail open) + the MCPService slice and runs `registry::resolve` — denials NAME the floor/service (observed live: `class "guarded" refuses: egress is closed…`, `MCPService "tickets" refuses: allow pattern "wipe_all" widens…`). Tag-laundering guard on MCPService UPDATE: drop-a-tag / widen-allow / shrink-exclude / repoint-endpoint refused NAMING live consumers (plain edit when none); permanent audit lines. The dead v1-era mode/surfaces mutate defaulting removed (it corrupted v2 version detection live). **CRITICAL FIX en route: the v2-spec ANNOTATION STASH in conversion** — storage is v2 and the operator writes through v1, so v2→v1→v2 round trips ERASED v2-only fields (observed live: a status patch deleted a stored services grant); down-conversion now stashes the full v2 spec on the v1 representation and up-conversion restores it (verbatim when the v1 surface is untouched; merged when edited) — proven by unit round-trip + live. **e2e `policy-ladder` PASS (3.0s)**; full catalogue 26/0 |
| P2-5 | Reload-vs-restart classifier + rolling restart path | config edit rolls with zero run loss (pins verified) | **Done(2026-08-30)** — `agentctl_operator::reload::classify`: JSON diff (dot-paths; lists atomic — layers replace) against the vendored RESTART_ONLY partition + the `a2a.principals` special case (reload-hostile despite not being in agentd's own list); the decision logs on every config apply (live: `reload-safe change (agent.instruction) — delivered as a drain-first rolling restart until upstream config-watch fires on kubelet swaps (U1)`), and the rolling restart is drain-first (SIGTERM drain 28s < 30s grace — in-flight runs finish under pins; mgmt-drain proves clean drains). Every daemon now renders `lifecycle.watch_config: true` (itself restart-only — born ready). **U1 VERIFIED RED live** (kind repro: armed watcher, kubelet ..data swap confirmed inside the pod, marker synced, ZERO events in 5+ min) → upstream root-caused it from our repro: the event filter matches only the config-file BASENAME, which a kubelet update never names (only the timestamped dir/..data_tmp/..data), and the in-repo "swap" test passes on a fabricated IN_CLOSE_WRITE leaf event the kubelet never emits. Two-line fix identified upstream, NOT scheduled — restart-on-every-change posture stands; the flip (reload-safe diffs skip the hash bump) is ready when a fixed build lands. Recorded in restart-only.json |
| P2-6 | Chart v2 umbrella (identity, mcpg-operator dep, profiles, values schema) | `helm install` dev profile → P1+P2 stack up | **Done(2026-08-30)** — `profiles/dev.yaml` (full stack, one cluster: bundled PG, identity service + AAuth provider role armed, operator house-provisioning wired; render-asserted to produce every P1+P2 workload) and `profiles/enterprise.yaml` (external PG, netpols, coarse gate, HA replicas, ServiceMonitors, pinned seal/signing keys); values.schema.json extended with the typed `identity.service.*` shape (providers require name/issuer/client_id, issuer pattern-checked); Chart description + README document the multi-version-CRD upgrade caveat (Helm never upgrades crds/) and the custom-namespace conversion-stanza patch. mcpg: NO hard chart dependency — mcpg publishes no chart repository yet; the tenant-mcpg wiring (P5) consumes whichever mcpg release is installed (recorded in Chart.yaml). The fresh-install lane is e2e/up.sh (e2e-base ⊇ dev profile) — the live cluster is that install |
| P2-7 | Agent handles + display names (`spec.handle`, org-unique at admission; gateway route + card resolution) | duplicate handle refused; `orgs/<org>/agents/<handle>` routes | **Done(2026-08-30)** — `spec.handle` + `spec.displayName` on Agent AND AgentFleet (additive on v1alpha1; carries into P2-1); `effective_handle` = handle-or-name, DNS-1123 syntax (`agent_api::{effective_handle,valid_handle}`); admission rung lists Agents+Fleets in the namespace and refuses a second holder naming the current one (fail-open only on transient list errors — CEL cannot express cross-object uniqueness); gateway org routes + cards resolve the path segment BY EFFECTIVE HANDLE (post-authn for RPC so anonymous probes cannot enumerate; 404 says "handle"). **Live in `org-route-user`**: routed + card via handle `helper` ≠ CR name, duplicate-handle create refused with "already held" |
| P2-8 | Trigger sugar over all 10 agentd start kinds → generated start-node workflows; shape inference; prerequisite wiring; CLI `create agent --schedule/…` | 10-row e2e matrix: provisions, fires, right workload shape | **Done(2026-08-30)** — `agent_config::v2::from_v2_spec`: every trigger compiles to a generated dialect-3 `main-<kind>` workflow (`start→work→done`, version:3 mandatory) with the source-audited field names PINNED BY TEST (subscribe = `server`+`uri`+`debounce_ms` — an unknown server passes `--validate-config` and silently never fires, so THIS compiler refuses it; webhook needs the `webhooks.listen` block — loopback bind, `methods` MUST be an array, rate grammar `<burst>/<per>s` seconds-only — caught live by the binary; stream auto-declares its `streams:` retention — undeclared = silent-never-fires; schedule prefers `cron` passthrough w/ `every` fallback; event field renamed for the YAML-1.1 trap). Shape rules: explicit wins; defaulted-daemon w/ only once/manual ⇒ Job; `shape: cron` needs CRON syntax (`every` can't be a CronJob) — the CronJob path uses agentd's own instruction sugar, no workflow. Operator: the Agent path now composes from the **v1alpha2 view** (storage read, stash-backed; in-process up-convert fallback), resolves `services[]` grants to MCPService endpoints (+credential env wiring), and the compiler's shape drives the workload kind. `agentctl create agent` — every kind a flag, combinable, shape mirrored client-side. **The ten-kind projection VALIDATES against the real 1.3.1 binary** (gated test, exact `-c -c` invocation). **Live e2e `trigger-matrix` PASS**: eight-trigger daemon → Deployment with the webhook POST and the 30s loop OBSERVED FIRING from the agent's own events; CLI `--once` → Job COMPLETES; CLI `--schedule` → CronJob with the exact expression. Full catalogue 27/0 |

## P3 — Durability plane

| ID | Item | Definition of done | Status |
|---|---|---|---|
| P3-1 | `state.*` service (checkpointer profile on PG via mcpg) | agentd `store.kind: mcp` passes upstream-style SIGKILL/restore matrix; p99 ≤ 50 ms in-cluster | **Done(2026-08-31) — UNPARKED on beta.26.** The mcpg #664 entity-vtable fix lets backend-sql (0.1.0-alpha.21) register (backend + both watch-strategies, no collision). **Live e2e `state-durability` PASS (34s)**: the 4-tool contract (seq-CAS put/get/list/delete, idempotent replay, {latest} conflict) AND a real `store.class: managed` agentd agent checkpointing through the service, surviving `kill -9` with zero restarts (no split-brain). Wire facts settled this cutover: state gateway SERVES TLS (`gateway.server.tls: {cert_path,key_path,min_tls_version}` — agentd refuses plaintext MCP off-loopback; cert from the chart CA the agent already trusts); the agent's store binding presents `x-mcpg-subject-id: orgs/<ns>/<agent>` (new `ResolvedMcp.static_headers`) → mcpg header-asserted tier (the config-declared-binding floor, exercised for the first time now that state.* bindings exist; per-tool rules shadow the global default) — the SAME seam P3-2's verified fence upgrades; state.* schemas allow the MCP-reserved `_meta` arg; beta.26 enforces unique JSON-RPC ids per session. |
| P3-2 | Server-side tenant fencing + `state.admin.snapshot/restore` | cross-agent key access provably impossible (test); snapshot/restore round-trip | **Done(2026-08-31)** — SERVER-SIDE FENCE now REAL (was interim-only). mcpg live-proved `param_exprs: {caller: "identity.subject_id"}` resolves at the header-asserted tier (the asserted x-mcpg-subject-id fills the same PluginIdentity.subject_id a verified JWT will), so the state.put/get/list/delete SQL keys every row on the CALLER's own subject (`WHERE starts_with(key, :caller||'/')`) — host-supplied, the args map can NEVER set it (param_exprs override caller args + `additionalProperties:false` refuses them; anonymous → subject_id null → matches nothing). A conforming agent physically cannot touch another agent's keys. **Live e2e (in `state-durability`)**: the driver asserting its own subject gets ABSENT on state.get and EMPTY on state.list of the managed agent's prefix (FENCE BREACH bails otherwise), while `state.admin.snapshot/restore` (unfenced, take an explicit prefix — the operator/control-plane admin surface; header-asserted interim, `governance.minimum_trust: verified` is the prod floor) cross prefixes and round-trip a snapshot (put→delete→restore→get). The malicious-asserter residual closes at the verified tier — a pure header→JWT swap, zero binding changes (mcpg-confirmed). |
| P3-3 | `artifacts.*` façade over content store (S3) | put/get/list with org quotas | **Done(2026-08-31)** — new BUSL crate `agentctl-artifacts`: the `artifacts.put/get/list` MCP backend over an S3-compatible content store (bundled single-node MinIO by default; `s3Endpoint` points at a managed S3). Blobs go to object storage, not the state Postgres. **Org-fenced**: every object is keyed under the caller's org prefix (first two segments of the host-asserted `x-mcpg-subject-id`, the same identity seam `state.*` uses); the caller supplies only a RELATIVE key, path traversal is refused, and no identity ⇒ fail-closed. **Per-org byte quota** (`ARTIFACTS_ORG_QUOTA_BYTES`) checked before each write. The S3 client is HAND-ROLLED on reqwest + SigV4 (sha2/hmac/hex) — every off-the-shelf S3 crate drags an OpenSSL/MPL/vulnerable-XML tail this rustls-only, deny-clean workspace won't carry; keys are safe-charset so path/query signing needs no encoder round-trip, and pagination is `start-after` (our own keys), never the opaque continuation token. Chart: `artifacts.enabled` renders MinIO (non-root, its own creds Secret) + the service + an ingress netpol; the service auto-creates its bucket on startup (retries until MinIO is up). **Live e2e `artifacts-flow` PASS (1.7s)**: put→get byte-identical round-trip, list, the ORG FENCE (a second org can neither get nor list the first org's key), an over-quota write refused with `quota_exceeded` (and nothing stored), and a no-identity call refused. |
| P3-4 | Store classes in Agent CRD (`ephemeral/local/managed`) + StatefulSet path | all three render + run | **Done(2026-08-31)** — all three render + run. `ephemeral` (default) → Deployment on an emptyDir; `managed` → the state-service checkpointer (P3-1); **`local` NOW IMPLEMENTED** → a single-replica StatefulSet whose volumeClaimTemplate provides a durable PVC for the agentd file store (new `StoreSpec.size`, default 1Gi; `ResolvedMcp`-independent — pure render). **Live e2e `store-classes` PASS**: ephemeral→Deployment, local→StatefulSet with a bound PVC that SURVIVES a `kill -9` pod delete (same claim uid, replacement pod re-attaches it — an emptyDir would be gone), managed via state-durability. The durability boundary is proven at the volume (the agentd image is distroless — no shell to write a marker). |
| P3-5 | Lifecycle verbs: backup/restore/migrate/stop/start/reset | `agentctl migrate` between nodes with zero run loss (managed) | **Done(2026-08-31)** — six state-plane lifecycle verbs on the aggregated management API (`management.agentctl.dev`, SAR-gated), CLI subcommands + apiserver handlers. **backup/restore/reset** drive the state service's admin tools (`state.admin.snapshot`/`restore`/**new `state.admin.purge`**) over the chart-CA-trusted mTLS hop the apiserver already has (reused the pod-control client — same CA issues the state cert; verifier is chain-only); the agent's backup unit is its `orgs/<ns>/<name>/` subject prefix (exactly the fence's `caller||'/'`). **stop/start** patch `spec.lifecycle.paused` (operator → replicas 0/back, P7-6). **migrate** = the DoD: verify `store.class=managed`, snapshot the checkpoint high-water per key, delete the pod, wait for the replacement Running, re-snapshot and ASSERT every key's seq preserved (never lost/regressed) — durable state lives in central Postgres so the pod swap is lossless; refuses ephemeral/local (node-local) with a 409. **Live e2e `lifecycle-verbs` PASS (6.5s)**: managed agent checkpoints → backup non-empty → migrate replaces the pod with all keys preserved → stop parks (replicas 0) → reset purges (backup empty) → restore round-trips the snapshot back → start wakes. RBAC: apiserver SA gains `pods delete` + `agents get/patch`; state-ingress netpol admits the apiserver; `AGENTCTL_STATE_URL` (base, apiserver appends `/mcp`). |
| P3-6 | State capacity benchmark (checkpoint QPS vs fleet size) | numbers in docs/benchmarks.md | **Done(2026-08-31)** — measured against the real state plane on mcpg beta.26, TLS + tenant fence active. New `bench` sweep `f` (`sweep_state_checkpoint`) drives a fleet of independent seq-CAS checkpoint streams (each fenced to its own subject prefix, self-healing CAS so a transient never cascade-fails) over the concurrency ladder 1/4/16/64 = fleet size. **Numbers in docs/benchmarks.md**: ~5 ms/checkpoint at low concurrency, saturating **~735 checkpoints/sec** single-replica with **zero CAS errors** at every level; the **p99 ≤ 50 ms budget holds up to ~4–8 writers/replica** (p99 17.9 ms at 4), then the bundled Postgres saturates (p99 206 ms at 64) — the SAME knee + single-replica floor as the durable coordination store, and scaled the same way (replicas + provisioned PG). Real finding folded in: the state gateway disables mcpg's per-IP anonymous rate limiter (`anonymous_rate_limit_per_min: 0`) — it throttles every below-verified caller incl. a real header-asserted agent, and is redundant behind the netpol perimeter + subject-prefix fence. |

## P4 — Supervisors + control MCP

| ID | Item | Definition of done | Status |
|---|---|---|---|
| P4-1 | System mcpg deployment + `control.*` bindings over apiserver | tool schemas reviewed; audited | **Done(2026-08-30)** — implemented as a FIRST-PARTY Rust MCP server (`agentctl-control`), not mcpg: the control surface is identity-critical (RFC 9421 jwt-scheme verify + `wl`-claim scoping) and Rust-only is locked; mcpg stays the TENANT tool plane (P5). Five audited tools (agents.list/get/status/resolve read; create = narrow builder, created-by stamped, admission still gates); auth = AAuth challenge → provider-JWKS token trust → cnf.jwk PoP; scope = the token's operator-registered namespace (no ns argument exists on the wire). Identity mints `wl` into aa-agent+jwt; org controller seeds per-ns `control` MCPService + `supervisor` AgentClass create-if-absent; supervisors render identity.aauth. Every call logged with the verified agent id (audit line). **Live e2e `control-mcp` PASS**: challenge, tamper-refusal, ns-scoped list, gated create, resolve, status, seeding |
| P4-2 | OBO chain for control calls (identity exchange, binding checks) | supervisor of a viewer cannot create (e2e); every call attributed | **Done(2026-08-30)** — binding-check design (no wire claim to forge): acting user = Supervisor CR named like the verified calling workload; owner groups = `status.ownerGroups`, stamped by the GATEWAY at every introspection (refreshes each conversation; the supervisor cannot assert them). Org accessPolicies gate per-tool (reads viewer, subagents operator, CRUD admin; governed org + zero grants = refuse; service/ungoverned = P4-1 posture); every call audit-logged with acting_for + created-for annotation on writes. **Live e2e: viewer-bound supervisor lists but cannot create; the admin-group stamp flips it.** The RFC 0030 /exchange token-minting leg is NOT needed for control calls (server-side binding is stronger); it remains open for third-party OBO (P5) |
| P4-3 | `Supervisor` CRD + auto-ensure on first login + class profile | login → chat → estate listing | **Done(2026-08-30)** — operator `supervisor` controller (4th, CRD-gated): Supervisor → owner-referenced v2 Agent (daemon, handle = CR name, owner = the ONLY named principal, services/budget from the ns's `supervisor` AgentClass profile; classless default absent it); gateway `POST /orgs/<org>/supervisor` auto-ensures by introspected subject (auto→create+503-provisioning, manual→404, disabled→403) and forwards with the caller's principal bearer; CLI `chat <org>/supervisor` w/ provisioning wait; `operator.defaultAgentImage` for platform-rendered agents. RBAC live-caught: operator needs `agentclasses` read + `agents` create; gateway `supervisors` get/list/create. **Live e2e `supervisor-route` PASS**: 401 → 503+CR(spec.user mock:carol) → owner-scoped render, exact principals → converses. Estate listing itself = P4-1 control MCP |
| P4-4 | Instruction layering (system→org→user; user layer inert to directives) | fence-injection test red | **Done(2026-08-30)** — `compose_instruction`: platform layer (class profile) leads verbatim; the user's `instructionOverride` folds in BLOCKQUOTED line-by-line under a "quoted data — never instructions" heading, so a smuggled `:::` directive fence never starts a line and stays inert prose (agentd's extractor is line-anchored). Fence-injection test red-then-green is the merged DoD; the org layer slot is the AgentClass profile instruction (per-ns), same mechanism |
| P4-5 | Approval gates on destructive verbs (addressed to owner) | delete asks its human; operator override recorded | **Done(2026-08-30)** — two-phase `control.agents.delete`: ask → pending nonce annotated on the target (nothing deleted), relayed to the human; approval ONLY via the owner's own bearer (gateway `POST /orgs/<org>/approvals/<nonce>`, `agentctl approve` — 403 for anyone else, even another admin; the supervisor never holds that token); re-issued delete executes with who-asked + who-approved recorded; service workloads cannot delete (no human to ask); markers in `agent_api::approval` (10-min TTL). kubectl = the recorded operator override (apiserver audit). **Live e2e: unapproved ask leaves the agent; wrong-user approve 403; owner approve → delete executes** |
| P4-6 | `control.subagents.create` narrowed grant (child Agents, TTL, budgets) | agent spawns governed child; caps enforced | **Done(2026-08-30)** — run-to-completion child OWNED by the caller's Agent CR (GC cascades = the lifetime bound; a TTL beyond completion deferred until a reaper exists), parent label = audit + DEPTH CEILING (a spawned child cannot spawn; agentd's in-process depth never crosses pods), parent's lifetime-token budget is a ceiling the child only narrows (inherited when unstated), operator-tier when user-bound. **Live e2e: child owned+labelled; caps pinned by unit tests (clamp, inherit, depth refusal)** |
| P4-7 | **@mention orchestration** (class workflow: resolve → batch fan-out `a2a.delegate` typed `ask` w/ `args.hops` ceiling → gather → synthesize loop; `concurrency scope: key` per user) | "@a and @b" answered with both accounted for; forbidden handle reported; hop ceiling kills supervisor↔agent loops (own counter — agentd depth does not cross instances) | **Done(2026-08-30)** — gateway rewrites bare-@ prose into the typed `mention` DataPart envelope (hops: 2; `@skill:` untouched); the supervisor renders the dialect-3 `mention` workflow (a2a start on the command w/ `access.grants: [mention]` — agentd refuses ungranted typed ops, NEW spec field; switch hop guard; foreach `a2a.delegate` fan-out `on_error: continue`; deterministic template gather; max_runs 1 = per-user serialization since a supervisor IS per-user) + `spec.peers[]` = the OWNER-reachable estate. Peer dials are DIRECT target-mTLS **as the owner** (per-target bearer copies in `<name>-peer-bearers`, static-auth secret-file refs; a target not naming the owner is omitted ⇒ mention = the owner's own reach, and @ghost reports as the no-such-peer slot). Whole render binary-validated in the gated test. **Live e2e `mention-orchestration` PASS**: both answered, @ghost reported, hops=0 short-circuits. Also landed: admission policy-inert updates (finalizer-removal deadlock fix, bitten live), `{name, file}` workflow refs (bare `{file}` refused by the binary — latent), foreach body `{steps:{…}}` runtime-vs-validate strictness recorded |

## P5 — Capability plane

| ID | Item | Definition of done | Status |
|---|---|---|---|
| P5-1 | Tenant mcpg per org (operator-provisioned via mcpg CRs) | org create brings its gateway; catalog rendered from MCPService entries | **Done(2026-08-30)** — provisioned by OUR operator directly (no mcpg-operator dependency; Rust-only stands): the org controller renders `agentctl-mcpg` ConfigMap/Deployment/Service per managed ns (org-owned, GC cascades) from the blessed GATEWAY image, PROXY-ONLY (`mcp.federations[]`, zero plugins — the broken OCI lane never enters the boot path). Registry entries federate under `<entry>.` prefixes narrowed by allow/exclude (mcpg's exact\|*\|prefix* grammar; inner wildcards drop with a warning); `control` NEVER federates (identity laundering, pinned by test); header-asserted callers + netpol perimeter (P5-2 upgrades to JWKS); HOT catalog via config_watch. **Live e2e `tenant-mcpg` PASS**: org brings the gateway, workstats.work.stats visible and callable through governance, nothing else leaks |
| P5-2 | AAuth verification chain at mcpg (identity JWKS) | unsigned/foreign-signed calls refused | **Done(2026-08-30)** — the tenant gateway's floor rises to VERIFIED: `governance.access.jwks` inlines the Agent Provider's published JWKS (EdDSA/OKP — supported by mcpg's verifier; no runtime fetch), issuer-pinned and AUDIENCE-BOUND per org (`mcpg:<ns>` — a token for org A dies at org B). Identity mints the tokens (`POST /admin/mcpg-token`, operator channel; `sub` = workload, provider-key-signed — one key, one JWKS, three verifier families). Identity unreachable at render ⇒ header tier + warning (a blip degrades the floor, never the plane). **Live e2e (in `tenant-mcpg`)**: unsigned refused, wrong-audience refused, own-org verified caller round-trips |
| P5-3 | Our Apache CredentialIssuer plugin → identity `/exchange` | per-user token injected upstream; cache + refresh observed over simulated days | **Done(2026-08-31)** — plugin half PIVOTED to mcpg's own symbol-verified `credential-oauth-token-exchange` (their catalog rebuild; single-entity, loads on blessed beta.24 — nothing for us to write; BUSL: e2e runs under `license.non_production_use`, production needs an mcpg entitlement). IDENTITY: `/v1/exchange` (RFC 8693, form wire matched to the plugin byte-for-byte) with a subject ladder — our own EdDSA JWT (acting user = operator-stamped `usr` claim, org from `aud`; SELF-AUTHENTICATING), A2A principal bearer (custody hash → user+org), bare user (admin-only) — over CUSTODY connections (`/admin/connections`, sealed AAD-bound rows, static + oauth_refresh kinds with refresh-token ROTATION persisted before return) + process cache with margin (`x-agentctl-exchange: mint|cache|refresh`) + `/metrics`. Simulated-days unit tests: 48h hourly static cycle, 7-day rotating-refresh-token provider (replay of a rotated token = invalid_grant). OPERATOR: `auth.mode: obo` MCPService → `oauth_impersonation` federation + plugin block (drops fail-closed unless plugin wired AND verified tier — impersonation never dials bare); emptyDir at `/home/mcpg/.cache` (OCI pull vs read-only rootfs); ndots:1. **Live e2e `obo-exchange` PASS**: mint→cache→user-less refusal on the wire, per-user secret injected upstream (echo witness saw the CUSTODY credential, not the caller JWT), user-less caller fails closed at the credential hop, revocation kills the next mint. FINDING reported to mcpg: their host credential cache keys on resolved subject (not raw bearer) — sub↔usr must stay 1:1, which per-user supervisors guarantee. |
| P5-4 | Connections flow (consent, `connection_required` → HITL card) | user connects a provider once; agent proceeds | **Done(2026-08-31)** — the provider catalog IS the identity providers list. IDENTITY: `/v1/connections/start` (caller = the USER's own validated identity token, or admin naming one; RFC 8628 with `offline_access` APPENDED — logins never ask for offline power) + `/v1/connections/poll` (redeems the device code, REQUIRES a refresh token, seals it into custody as the (org,user,provider) connection with the provider's token_endpoint/client_id; no token ever leaves this path; wrong-poller guards both directions). `/v1/exchange`'s no-connection refusal now carries the machine-readable CONNECT CARD (`connection_required: {org,user,provider,connect}` beside the RFC-legal `invalid_target` — mcpg surfaces the code, agentctl-native surfaces render the card; the in-conversation card rides P5-6's agentd gate bridge). CLI: **`agentctl connect <provider> --org <org>`** (session-authenticated, prints consent URI+code, polls to done). e2e mock-idp upgraded to a REAL device flow (auto-approve on 2nd poll, 8s access tokens, ROTATING refresh tokens). **Live e2e `connections-flow` PASS (24.5s)**: pre-consent exchange refuses with the exact card incl. the CLI one-liner → the REAL `agentctl connect` binary completes consent → custody row (oauth_refresh, secret-free listing) → the SAME user's gateway call proceeds with a refreshed per-user token injected upstream → 10s later a NEW token appears (refresh + rotation live — days compressed to one expiry). |
| P5-5 | `sandbox.*` backend plugin (K8s Jobs + warm pool, RuntimeClass optional) | code run with caps, no network, artifacts I/O; threat-model doc | **Done(2026-08-31)** — new BUSL crate **`agentctl-sandbox`**: the `sandbox.run` MCP backend (register as an org MCPService to federate through the tenant gateway). Code runs in SINGLE-USE pods in a dedicated deny-all **cell namespace**: every run leases a warm pod (per-language pool keeper hides start latency), execs a base64-wrapped script (no shell interpolation; stdin + input files in, stdout + declared `out_files` out, all server-capped), then DELETES the pod — no cross-run contamination. **Seven independent containment layers** (threat model at docs/v2/sandbox-threat-model.md): deny-all NetworkPolicy, `automountServiceAccountToken:false` (no cluster creds inside even on full container ownership), hardened SecurityContext (non-root/caps-dropped/RO-rootfs/seccomp), CPU/mem/wall-clock caps (timeout = pod deletion), single-use pods, output caps + bare-name guard, optional `runtimeClassName` (Kata/gVisor) for kernel isolation. The runner's only power is pod-lifecycle + exec in its OWN cell namespace (Role; TRAP: kube-rs's WS exec is a GET upgrade → needs `get` AND `create` on pods/exec). **Live e2e `sandbox-run` PASS**: code runs with stdin+file+artifact and exit code; a probe finds NO service-account token mounted; an over-time run is killed; the network-egress-denied leg is Calico-guarded (kindnet can't enforce). Chart `sandbox.enabled` + build-matrix entry + Dockerfile. |
| P5-6 | HITL fabric (mcpg approvals + agentd gate bridge + channel registry) | gate raised in agentd answered from Slack under the right identity | **Done(2026-08-31)** — the gate bridge IS the gateway (no extra service): agentd's `human` op parks the SAME task the command created at INPUT_REQUIRED with the question in `status.message` (wire facts source-verified with the agentd session: answers are PLAIN TEXT continuations `message.taskId` with schema coercion + re-ask on mismatch; wrong principal → -32602 with the gate OPEN; operator answers carry `via: operator_override`; an ADDRESSED gate is NEVER auto-answered whatever the approval policy — the hard human guarantee; `reply_uri` refuses at load). RENDER: `spec.approval.policy` → agentd `agent.approval` (+ matched `ask_human_fallback`: ask→wait, deny→fail) **and `interface: {enabled: true}` — THE human channel** (`available = interface.enabled && a2a_sink`; without it every gate parks channel-less forever, so `spec.approval` without `expose.a2a` now REFUSES at compile). GATEWAY: task store keeps `status.message` (the question), `tasks/get` serves store-first only for TERMINAL tasks (a stale row must never mask a live gate), persistence covers both method spellings and GetTask refreshes, and the TRANSITION into input-required fans out to the CHANNEL REGISTRY — `spec.approval.hitl` (AgentClass `hitl` fallback) `webhook:<url>` entries (a Slack incoming webhook IS one) through the SSRF-guarded push lane (`gateway.allowPrivateWebhooks` dev override for in-cluster sinks). **Live e2e `hitl-gate` PASS (10.6s)**: park → question on the wire + channel notified {taskId, question} → non-addressed principal refused, gate open → the addressed user's plain-text continuation through the org route under HER OWN identity completes the run with the decision in the output. mcpg-side PendingApproval notifier plugins + Slack-identity mapping remain the upstream half of RFC 0031 (additive; the agentctl fabric stands without them). |
| P5-7 | `work.*` fabric tools (coordination re-founding) | lease/ack/nack/DLQ + replay e2e | **Done(2026-08-30)** — the RFC 0022 fabric stands as the work plane: work.{submit,claim,renew,ack,release,stats,result,deadletter} + `work://pending` / `dlq://items` resources, memory + Postgres stores, source-IP attestation + mTLS lanes (sec-coord-* green). The DoD's lease/ack/nack(release)/DLQ/replay are ALL live: e2e `claim-atomic`/`claim-dedupe`/`claim-lease-expiry`/`claim-scale-zero` + **`work-redelivery`** (crash-mid-lease → exact-unit redelivery → ack → work.result; poison → DLQ → admin requeue(replay) → completes) + the result/deadletter wire tests. CLI dlq verbs folded into P7-5 hardening |

## P6 — Fleets & scaling v2

| ID | Item | Definition of done | Status |
|---|---|---|---|
| P6-1 | AgentFleet v1alpha2 + static strategy (vars overlays, singletons) | 3-replica fleet, nightly singleton fires once | **Done(2026-08-30)** — `spec.partitioning` (static \| dispatcher \| workqueue) + `static.{vars[], defaults, singletons[]}`; ONE shared document + per-ordinal `member-<n>.json` overlays (vars-only, third `-c` keyed by the pod-index label) + singleton entries `armed: "{{config.is_lead}}"` folding to a real bool on the lead — no leader election. Fleet composes through the TRIGGER COMPILER from the v2 view (the v1 path dropped v2 template fields); fleet conversion stash now merge-preserves. TRAP pinned in the gated binary test: agentd types each `-c` file independently — referenced vars MUST default in the base layer. **Live e2e `fleet-static` PASS**: 3/3, distinct member vars, the `loop` singleton (same fold as a nightly cron) firing on member 0 ONLY |
| P6-2 | Guarded re-partition (drain-first resize) | resize under load loses nothing | **Done(2026-08-30)** — the RFC 0022 quiesce-then-flip machinery (shipped v1.x) now LIVE-PROVEN: e2e `shard-resize` observes the guard engage (Ready=False/Resizing — a naive rolling update never surfaces it), complete at the new modulus (annotation + 3/3), and return Ready. Fixed en route: the fleet readiness readback had 403'd silently FOREVER (`statefulsets/status` needs its own RBAC rule; the Err was swallowed to 0 and nothing had ever asserted fleet Ready) + a 10s converging requeue (the owns() watch can quiesce before final readiness) |
| P6-3 | Dispatcher strategy (owner + workers + fleet route) | delegation fan-out e2e | **Done(2026-08-30)** — the RFC 0022 engine (coordinator render + fleet A2A route + member selection: task affinity → coordinator front door → worker round-robin) plus the missing piece: the coordinator's `worker` peer used to dial the SAME front-doored route (a structural self-loop); the gateway now serves the `/fleets/<ns>/<name>/workers` tier (never the coordinator) and the peer renders against it. `partitioning.strategy: dispatcher` requires spec.coordinator (refused loudly). **Live e2e `dispatcher-fanout` PASS**: fleet route → coordinator workflow → `a2a.delegate peer=worker` → a worker's own run events show the fanned-out ask |
| P6-4 | Workqueue strategy over `work.*` | crash mid-lease redelivers exactly the leased item | **Done(2026-08-30)** — `partitioning.strategy: workqueue` maps onto the claim work-fabric (refused without scaling.mode claim); **e2e `work-redelivery` PASS**: holder claims and VANISHES mid-lease → the server re-offers (its own backlog counter) → the SAME unit re-grants to the same claim_key as attempt 2 → survivor acks, `work.result` shows the outcome; poison unit (max_attempts 1) dead-letters as exactly itself → admin requeue → completes. work.result + work.deadletter list/requeue/drop wire-tested (were store-only). CLI dlq verbs still open (folded into P7-5 hardening) |
| P6-5 | Scaler v2 (inbox_pending/pressure/queue-depth; breaker damping; scale-from-zero) | webhook agent 0→1 on first delivery | **Done(2026-08-31)** — TWO lanes. (1) **Webhook scale-from-zero rides the park/wake lane, not KEDA** (only the hooks proxy can observe a delivery at zero replicas): `lifecycle.idleParkSeconds` parks a quiet webhook daemon to 0 replicas (user pause always wins, never auto-clears); the gateway stamps `agentctl.dev/last-delivery` on every delivery attempt — the FORCED stamp on the no-replica 503+Retry-After branch IS the wake — and the operator's half-window sweep flips replicas back; **live e2e `webhook-scale-zero` PASS (31s)**: park → 503+Retry-After → sender's retry loop lands on the woken pod, workflow fires. (2) **Signal v2 for claim fleets**: `spec.scaling.target.metric` finally honored — `backlog` (coordination, default) or **`inbox_pending`** (the metrics registry's scaler_guidance.primary: the scaler lists the fleet's member pods by label and sums `agent_inbox_pending` off their probes-port /metrics; new pods-get/list RBAC); plus **downscale damping** — `IsActive` flips false only after `SCALER_DOWNSCALE_STABLE_READS` (default 3) consecutive quiet reads while activation stays immediate, on top of the existing hold-last-on-error; unit-pinned (token parse + typo-degrades-to-backlog, exposition summing incl. wrong-port→None, damping state machine). `claim-scale-zero` regression green with the new ScaledObject metadata. `pressure_level`/`turns_queued` stay listed secondaries (additive tokens when a target profile needs them); breaker-open damping input has no upstream metric yet (registry gap — noted). |
| P6-6 | Per-fleet budget metering + policies | fleet window breach pauses intake | **Done(2026-08-30)** — `spec.budget {kind, maxUnits, windowSeconds}` on the v2 fleet, enforced against the P7-4 METERING EXPORT (the operator reads the same SAR-gated aggregated-API surface any billing consumer uses; fail-open on metering outage — billing being down never pauses workloads). Breach → replicas 0 + KEDA paused-replicas pin + `Ready=False/BudgetExceeded`; the pause is LOSS-FREE by the work fabric's redelivery; the window sliding past resumes the fleet unaided (half-window sweep leash). `tokens` windows activate the day the token source lands (the kind is the metering vocabulary's). **Live e2e `fleet-budget` PASS**: 4 metered requests breach a 3-unit window → paused → resumes 2/2 |

## P7 — Productization

| ID | Item | Definition of done | Status |
|---|---|---|---|
| P7-1 | Webhook exposure GA (`hooks.<domain>`, `agentctl expose`, Gateway-API profile) | external delivery e2e with HMAC + tenant limits | **Done(2026-08-31)** — the GATEWAY is the data plane (RFC 0029 §5): `ANY /hooks/{ns}/{name}/<path>` gates on declared exposure (`expose.webhooks[].path` ∧ a webhook trigger; admission refuses exposure-without-trigger), enforces method/size/rate ceilings (`gateway.hooks.{ratePerMinute,maxBodyBytes}`), forwards verbatim to agentd's listener and passes its verdict through; metered (`webhook_deliveries`) + counted (`agentctl_gateway_hooks_{forwarded,refused,unreachable}_total`); no ready replica ⇒ 503+Retry-After — P6-5's wake seam. AGENTD SIDE (wire facts verified with the agentd session): listener binds the pod network over the agent's own certs (plaintext off-loopback is refused), and EVERY route needs auth — so webhook triggers are **HMAC BY DEFAULT** (`auth: none` refuses at compile; secrets = operator-provisioned `<name>-hooks` Secret, PRESERVED across reconciles, mounted secret-file refs), signature = `X-Signature: sha256=<lowercase-hex HMAC-SHA256(secret, raw body)>` over body bytes only (NO replay protection in the MAC — `idempotency:` is the dedup tool), 401 for bad AND absent signatures, method check precedes auth (405 leaks route existence — upstream behavior), and `auth.hmac.algo` is parsed-but-IGNORED upstream (silent-downgrade trap; we pin `sha256` explicitly + test). CLI: **`agentctl expose webhook <ns>/<name> --path … [--show-secret]`**. **Live e2e `hooks-ingress` PASS**: admission refusal, signed delivery through the gateway fires the workflow, bad-sig 401 (agentd), GET 405 / unknown path 404 / 2MiB 413 (gateway). Vanity `hooks.<domain>` fronting = point the edge at the gateway route; the values-flagged Gateway-API `HTTPRoute` emission profile is deferred (the gateway profile is GA). |
| P7-2 | Dashboards + alert rules (fleet health, budgets, gates, exchange latency) | shipped in chart | Done — operator publishes per-fleet gauges (`agentctl_operator_fleet_{ready,desired}_replicas`, `_budget_used`, `_budget_paused`); gateway counts approval outcomes (`agentctl_gateway_approvals_total{outcome}`); dashboard gains Fleets & Budgets + Work fabric & approvals rows; PrometheusRule adds FleetBudgetPaused / FleetDegraded / WorkDeadletterGrowth / ApprovalsRefusedSpike. Exchange panels (outcomes + mint latency) landed with P5-3. |
| P7-3 | Audit pipeline end-to-end (gateway/apiserver/identity/mcpg/agentd streams) | one queryable trail for a full OBO tool call | **Done(2026-08-31)** — Apache crate **`agentctl-audit`** (`audit/v1`: attributed {org,ns,workload,user?} records with `trail_id`/`task_id` correlation; PG sink OFF the request path; filtered query). EMITTERS: gateway (`a2a.request` per routed RPC with the `x-agentctl-trail`/`x-request-id` trail — minted at the first hop, FORWARDED upstream; `task.state` per state TRANSITION = the agentd stream; `gate.notified`; `hook.delivery`; `approval.decision`), identity (`identity.exchange` every redeem incl. cache/refused with provider dims, `identity.consent`, `identity.connection_revoked`). MCPG STREAM: their only audit sink is file-in-pod, so the tenant gateway grows an **`agentctl-audit-shipper` sidecar** (Apache bin: offset-durable tail of the hash-chained file, complete-lines-only, batches into identity's **`/v1/audit/ingest`** — self-authenticating operator-minted workload JWT with the dedicated `agentctl:audit-ingest` audience, org attribution FORCED from the token; `event_id`/`prev_event_hash` ride as dims so chain verification survives). MANAGEMENT API: `GET …/audit/query?from&to&org&user&action&trail&task` (SAR `audit`/`query`) + the ingest door. **Live e2e `audit-trail` PASS**: one OBO tool call → ONE management-API query returns identity's exchange row (the honest **(user, provider, window)** join — mcpg's host cache makes one redeem serve many calls) AND the shipped hash-chained mcpg records org-forced into the same store. TWO upstream defects found live + confirmed-in-source by mcpg, fixes riding their HTTP-sink wave: `x-request-id` never reaches the AuditEvent (new `upstream_request_id` field coming) and **a tools/call with zero gate plugins writes NO audit record at all** (guard bug, not federation-specific); e2e tightens to tool.call + request-id joins when those land, and their first-class `dev.mcpg.audit.http` sink (contract: POST JSON array, bearer, 2xx=committed — approved upstream) will retire the sidecar. |
| P7-4 | **Billing-ready metering** — versioned event vocabulary (agent-hours, tokens by tier, tool calls by MCPService, OBO exchanges, sandbox CPU-s, state/artifact bytes, webhook/A2A counts, gates, seats, feature enablement), all attributed {org, group, user, agent} | durable events + Prometheus counters + apiserver aggregation/export (CSV/JSON by period); a sample invoice computable from export alone | **Done(2026-08-30, sources landing incrementally)** — `agentctl-metering` (Apache: the vocabulary is a public contract): `metering/v1` events attributed {org, namespace, workload, user?}+dims, quantity+unit; ALL the row's kinds DEFINED (tokens/agent-seconds/sandbox/state-bytes/gates pending their sources — agentd usage export, P5-5 — schema fixed now so arrivals are additive). Durable PG sink (fire-and-forget, never on the request path; prom counters stay the ops signal) + period aggregation + RFC4180 CSV. GATEWAY emits at the chokepoint (a2a_requests per routed RPC, supervisor_conversations per routed SendMessage, user-attributed on org routes); APISERVER serves `GET …/metering/export?from&to&format` (SAR-gated `metering`/`export`). **Live e2e `metering-export` PASS**: billable conversation → attributed JSON+CSV rows → the org's invoice summed from the export alone |
| P7-5 | Managed-service profile docs + API hardening pass (req. 27) | "build on agentctl" guide | **Done(2026-08-31)** — THREE deliverables. (1) **The guide**: docs/v2/build-on-agentctl.md — what the platform gives a team building their OWN managed agent service (the Svix-for-agents framing), the four integration boundaries (management API / CRDs / A2A gateway / tenant mcpg), a multi-tenant hardening checklist (policy CNI first, seal key, apiToken, verified tier, budgets, audit shipping, Kata, PodSecurity), and the API-hardening the platform already applies. (2) **CLI dlq verbs** (folded from P5-7/P6-4): `agentctl dlq list|requeue|drop` over the coordination `work.deadletter` MCP surface (URL/token flags + env). (3) **Config-hash relaxation** — the operator's pod-template annotation now stamps `restart_hash` over agentd 1.3.1's authoritative `RESTART_ONLY_PATHS` ∪ {`a2a.principals`, `webhooks`} (the two paths agentd neither refuses NOR reloads — a silent-strand trap the agentd session confirmed in source; the all-or-nothing reload rule makes the union the safe floor). Hot-reloadable changes (instruction/persona, workflows, intelligence, mcp.servers, limits/interface/…) now ride the ConfigMap and hot-reload with NO pod roll; **verified live: an instruction rewrite keeps the pod hash stable while an `a2a.principals` change rolls the pod**; unit-pinned. Plus the **HotReload=False/InotifyExhausted** Agent condition surfacing agentd's `config.watch.error` errno-24 (node inotify exhaustion → watcher exits, silent staleness) — keyed on event+errno, not message text. |
| P7-6 | Supervisor scale-to-zero (gateway park/wake) | dormant supervisors cost ~0 | **Done(2026-08-30)** — the park clock IS the wake signal: the gateway stamps `status.lastConversation` on every supervisor touch (30s rate-limited); the operator's sweep (half-window leash) parks past `operator.supervisorIdleParkSeconds` and the next touch unparks through the ordinary 503-provisioning flow. `lifecycle.paused` became REAL en route (paused v2 daemons render at zero replicas — previously declared-only; config/identity/peers/Secrets stay). **Live e2e `supervisor-park` PASS (42.7s)**: converse → Parked at 0 replicas → one message wakes to a live answer |
| P7-7 | OCI bundles for registry sets; docs site refresh; v2 GA checklist | — | **Done(2026-08-31)** — the program capstone: **docs/v2/GA-checklist.md** — the honest GA state (every plane's status traced to its e2e scenario; 45/49 catalogue; the fail-closed/attribution/supply-chain gates; the one blocked area — state durability P3-2..6 — scoped as upstream-only, built to the P3-1 boundary, self-unskipping when mcpg's pairing boots). Docs-site refresh noted as a presentation pass, not a capability. **OCI-bundle registry-set resolution LANDED (2026-09-01, commit 644f262)**: the operator pulls a digest-pinned `workflows[].setRef` WorkflowSet (hand-rolled OCI pull on reqwest + sha2, manifest+layer digest-verified, ndots-safe in-cluster hosts), projects the workflow docs into the rendered config, and admission validates the digest-pin (was a blanket refusal). Live e2e `oci-bundles`. |

## PM — Multi-cluster (held at Draft)

| ID | Item | Status |
|---|---|---|
| PM-1 | RFC 0035 design review post-P6 (single-cluster seams re-checked) | Deferred |
| PM-2 | Hub/spoke sync agent + `Cluster` CR | Deferred |
| PM-3 | Gateway federation + cross-cluster migrate | Deferred |
| PM-4 | Supervisor federation (front supervisor ↔ remote/per-cloud supervisors as A2A peers; same `ask` command + `args.hops` ceiling) | Deferred |

## Upstream asks (file in P0-6; re-check each agentd/mcpg release)

| ID | Upstream | Ask | Status |
|---|---|---|---|
| U1 | agentd | A2A admin `reload` verb, or verified `--watch-config` on kubelet ConfigMap symlink swaps | Delivered 2026-08-30 (batch, priority #1) |
| U2 | agentd | A2A TLS hot-rotation (webhook listener already rotates) | Delivered 2026-08-30 (batch) |
| U3 | agentd | `--capabilities`: restore contract/surfaces info; include webhooks + stream/webhook start kinds; tier catalogue | Delivered 2026-08-30 (batch) |
| U4 | agentd | `run_until: auto` webhook/stream misclassification; unify the three long-lived lists | Delivered 2026-08-30 (batch) |
| U5 | agentd | published config schema catch-up ($defs Service.kind/methods/breaker, A2aPeer.service); image license label (says Apache-2.0, is AGPL) | Delivered 2026-08-30 (batch) |
| U6 | mcpg | store-profile MCP tool façade upstreamable; sandbox backend (their RFC 0022) offered back | Todo |
| U7 | aauth/agentd | delegation chains; agentd inbound verification (A2A leg) — carries RFCs 0023–0025 asks forward | Todo |
| U9 | agentd | a stock-image way to stage the binary for out-of-image use | **Done(2026-08-30) — declined upstream** (the binary stays single-purpose; packaging is not its problem). Resolution: chart supports BOTH `binaryValidation.mode: staging` (busybox-derived image) and `mode: imageVolume` (KEP-4639 image-typed volume, beta ~K8s 1.33 + containerd 2.0 — mounts the STOCK signed image, nothing derived). Bonus finding folded into the contract: --validate-config enforces secret resolution ONLY in header maps (intelligence.token passes unresolved — upstream defect raised); admission gained a referenced-Secret existence rung in response |
| U8 | agentd | interface composer `@` completion inserted bare `@name`, which never matched the `@skill:` default prefix (skill never preloaded) and collided with conversational agent handles | **Done(2026-08-30)** — fixed upstream on main (`bc203942`, unreleased): composer inserts the full `@skill:<name>`; `docs/interface.md` corrected (it had documented the broken bare form as working). Bare `@name` is formally free for agent handles. **Caveat**: the prefix is hardcoded in the composer (the daemon never sends `skills.reference_prefix` to clients) — agentctl must not project a custom prefix (renderer rule, RFC 0032 §4.6) |
| U10 | agentd | `store.kind: mcp` data-plane, two related asks. (a) **Run-cursor GC**: a loop/daemon writes one `run/<workflow>-<runid>` key per tick and never reclaims completed ones, so a managed prefix grows without bound over uptime (retain only live / last-N cursors). (b) **Chunked restore**: agentd restores at boot with a single `state.get`, which rides one mcpg FFI frame (256 KiB) — a checkpoint agentd itself wrote larger than that (e.g. a big `context`, which the 4 MiB write body permits) would fail agentd's own restore. agentctl's control-plane READ is now UNLIMITED (2026-09-01: keys-only `state.admin.list`, keyset-paginated `state.admin.snapshot`, and per-value `state.admin.read_chunk`; `agentctl backup`/`migrate` walk pages and stream oversized values — docs/v2/known-limits.md), but only agentd can bound the store's growth (a) and chunk its own runtime restore (b). | Todo |

## Deferred register (decisions consciously not taken now)

| Item | Where noted | Revisit |
|---|---|---|
| Eval/testing service for agent changes (agentic SDLC beyond canary+conformance) | RFC 0026 §2 | post-GA |
| First-party web console (beyond thin agentd-ui) | RFC 0026 §2 | post-GA |
| LLM traffic through a gateway | ADR-0004 | with data |
| Gateway-side DLP/message policy | RFC 0029 §9 | P7+ |
| NATS-backed work fabric | RFC 0031 §8 | v2.1 |
| Per-org KMS keys (BYOK) | RFC 0028 §10 | managed profile |
| Virtual-cluster tenancy | ADR-0010 / RFC 0035 | PM |
| Public vendor-neutral ACC v2 spec | RFC 0026 §6 | post-GA |
| KEDA scaler backend | RFC 0034 §7 | v2.1 |
| Cross-org registry export/import | RFC 0032 §7 | v2.1 |

## Risk register (top 5, watched)

1. **Two pre-1.0 upstreams** (agentd resets, mcpg in-place ABI breaks) —
   mitigation: digest pinning, lockstep CI matrix, contract fixtures per version.
2. **State service on the hot path** (synchronous reactor I/O) — mitigation:
   p99 budget + benchmark gate (P3-6) before `managed` becomes default.
3. **Identity is a crown jewel** — mitigation: smallest surface, strictest
   netpol, envelope crypto, external review before P5 exit.
4. **OBO provider variance** (IdPs differ on 8693/ID-JAG) — mitigation:
   connection-based fallback is always available; capability flags per provider.
5. **Scope breadth** — mitigation: phase gates are demos, not documents; PM held
   in Draft until P6.
