export type Stat = {
  // Either a count-up number (value) or a static display string (display).
  value?: number;
  display?: string;
  decimals?: number;
  prefix?: string;
  suffix?: string;
  label: string;
  detail: string;
};

// Flip to `false` to hide the Benchmarks section until docs/benchmarks.md is
// generated by the Phase 4 harness. The figures below are the already-verified
// coordination-correctness + scale results from the e2e + scale benchmark.
export const BENCHMARKS_ENABLED = true;

export const BENCHMARKS: Stat[] = [
  {
    value: 72,
    label: "concurrent claim attempts",
    detail: "raced against one contended work-item set on /mcp",
  },
  {
    value: 12,
    label: "grants",
    detail: "exactly one winner per work item — deduped by claim_key",
  },
  {
    value: 0,
    label: "double-grants",
    detail: "atomic single-grant held under full contention",
  },
  {
    display: "0→N",
    label: "scale-from-zero",
    detail: "KEDA scales a claim fleet up on backlog and back to zero",
  },
];

export const BENCHMARKS_CAVEAT =
  "Measured on a single-node kind cluster driving the agentd v1.0.0 reference agent " +
  "(ghcr.io/agentd-dev/agentd:1.0.0, a ~1.3 MB static binary). These are correctness " +
  "results and host-bound trends from the re-runnable e2e + scale harness — not " +
  "capacity guarantees. Re-run on a real multi-node cluster for production numbers.";
